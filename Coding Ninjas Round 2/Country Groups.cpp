#include<bits/stdc++.h>
#define ll long long
using namespace std;


int main()
{
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
    cout.tie(NULL);
    int n,p;
    cin>>n>>p;
    for(ll i=0;i<p;i++)
    {
        ll x,y;
        cin>>x>>y;
    }
    cout<<p-1<<endl;
    
    
    return 0;
}